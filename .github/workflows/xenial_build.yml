name: Xenial-Build

on:
  push:
    branches:
      - master
  pull_request:
  schedule:
    - cron: '0 0 * * 0'

jobs:
  industrial_ci:
    name: Melodic
    runs-on: ubuntu-latest
    env:
      CI_NAME: Xenial-Build
      OS_NAME: ubuntu
      OS_CODE_NAME: xenial
      ROS_DISTRO: kinetic
      ROS_REPO: main
      BEFORE_BUILD_TARGET_WS: './ci/.ci_install_phoxi_sdk_xenial.sh'
    steps:
      - uses: actions/checkout@v2
      - uses: 'ros-industrial/industrial_ci@master'
        env: ${{env}}
